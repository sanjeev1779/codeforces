#include<iostream>
#include<vector>
#include<string.h>
#include<stdio.h>
#include<map>
#include<math.h>
#include<algorithm>
#define LL long long
#define P(N) printf("%d\n",N);
#define S(N) scanf("%d",&N);
#define pb push_back
#define mp make_pair
using namespace std;
main()
{
    int i,j,K,N,M;
    S(N);S(M);S(K);
    int store[305][305];
    memset(store,0,sizeof(store));
    for(i=1;i<=N;i++)
    {
        scanf("%s",str);
        for(j=1;j<=M;j++)
        {
            arr[i][j]=str[j-1];
            store[i][j]=store[i][j-1]+;
        }
    }


return 0;
}
